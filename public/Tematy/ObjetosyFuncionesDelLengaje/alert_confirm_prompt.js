/* 
.- Alert, confirm & prompt

.- Window.alert() [ https://developer.mozilla.org/es/docs/Web/API/Window/alert ]
? El método Window.alert() muestra un diálogo de alerta con contenido opcional especificado y un botón OK (Aceptar).
! El diálogo alert debe ser usado para mensajes que no requieren respuesta por parte del usuario, otra que la aceptación del mensaje.

.- Window.confirm() [ https://developer.mozilla.org/es/docs/Web/API/Window/confirm ]
? El método Window.confirm() muestra una ventana de diálogo con un mensaje opcional y dos botones, Aceptar y Cancelar.
? El método regresa un resultado boolean que el resultado de aceptar o rechazar la ventana emergente.

.- Window.prompt()  [ https://developer.mozilla.org/es/docs/Web/API/Window/prompt ]
? El método Window.prompt() muestra un diálogo con mensaje opcional, que solicita al usuario que introduzca un texto.
*/

/* let alerta = alert("Esto es una alerta!")

let confirmar = confirm("Esto es un confirm")

let aviso = prompt("Esto es un prompt y permite ingresar datos")

console.log(alerta);
console.log(confirmar);
console.log(aviso); */

/*
! Console clear
*/
console.clear()